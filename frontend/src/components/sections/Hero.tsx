import { motion } from 'framer-motion';
import { ArrowUpRight, Mail, FileText } from 'lucide-react';
import { MaterialButton } from '@/components/ui/MaterialButton';

const Hero: React.FC = () => {
  return (
    <section className="relative flex min-h-[100vh] w-full flex-col items-center justify-center px-4 pt-2020045.5</

</div>
    )}
  </section>
};

export default Hero;